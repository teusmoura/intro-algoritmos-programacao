/*
 * Le dois inteiros e imprime a soma.
 * Entrada: a (linha 1), b (linha 2). Saida: a + b
 */

#include <stdio.h>

int main(void) {
    int a, b;

    scanf("%d", &a);
    scanf("%d", &b);

    printf("%d\n", a + b);

    return 0;
}
